# addToEvent (Firestore event ledger)

Sibling of `addToTable`. When a component JSON has `addToEvent`, the submit ALSO
writes a keyed document into a named Firestore collection — in addition to the
existing spreadsheet Event row (unchanged).

## Flow
`saveSend` / `checkerSaveData` append the event string as the **4th `⬤` segment**
of `tb` (`add⬤update⬤delete⬤event`). At `historySync` (online), `tbParts[3]` →
`writeToEvent()`:
1. `autheniumDecode` → split by `◆` (separator[1]) into blocks.
2. `parseAddToEvent` — split `⭘` (sep[8]) → first part = collection, rest split at
   first `◼` (sep[2]) into key/value (`lib/firestore_repository/add_to_event.dart`).
3. `resolveValueTokens` — `◀N▶`=system/ref[0], `◁N▷`=form/ref[1] (shared with
   addToTable; extracted from `parseTableInput`).
4. Build the doc: auto-inject `et`/`p`/`ev` (Event-tab cols A/B/C — see Notes),
   THEN the DSL fields (token-resolved) so an explicit DSL field of the same key
   still wins; `_collection` is dropped (it is the write target). Beyond the three
   auto fields there is no blank-prefill — codes not written in the DSL are absent.
5. Write via `eventCollectionPath(line1, eventTableVid)`, batched atomically
   (`WriteBatch`). `tableVid` comes from the `tablevid` field, else the appCode
   default; `tablevid` is routing metadata only — read for the path then dropped
   from the stored doc. Two collection forms (line1 = the DSL's first segment):
   - **New `<tableDocId>//<subColl>`** (e.g. `84214220504259//event`) →
     `MobileTable/<vid>/tables/<tableDocId>/<subColl>/<autoId>` — a subcollection
     **parallel to `site`/`workforce`**, read back by `subscribeToMapCollection`.
     NO trailing `/content` (adding it yields a 6-segment, even, INVALID
     collection path that `.collection()` rejects → the write throws and is
     swallowed, so no doc appears).
   - **Legacy plain name** (e.g. `vtl.report-incident`) →
     `MobileTable/<vid>/tables/<getDocumentName(name)>/content/<autoId>` — the
     `/content` is REQUIRED here (a plain name alone is even/invalid).

## Notes
- `et`/`p`/`ev` (Event-tab cols A/B/C) are auto-injected onto EVERY event doc by
  `buildEventDoc`: `et`=Time (epoch ms, `eventRow[0]`/`timeReceived`), `p`=Route
  (`receivingPage`), `ev`=Data (raw content blob, `eventRow[2]`). DSL fields of the
  same key override. Col D (`ld`, Stored) stays server-only — generated by
  `processEvent2`, unavailable client-side. These cols still also ride the
  spreadsheet path (`appendToSheet`); the auto-inject only mirrors them into the
  keyed Firestore doc.
- `rf` `<no_request>` resolves at submit via `_resolveScreenTxMarkers` (screenTx key),
  same as addToTable.
- Token output is string. Exception: `r` is coerced to int when it parses cleanly
  (non-numeric `r` left as String). Other numeric-looking fields (`an`, `cv`, `sn`,
  …) stay String — they are IDs/hashes that must not lose precision.
- Offline-first: rides the same history queue as addToTable; the write happens at the
  next `historySync` (immediately if online).

## Not yet implemented
- Approval-chain `addToEvent` (FtzRowOfButton2 `_updateApprovalChain` /
  `createApprovalEvent`, parent ref-id for `rf`) — deferred to a separate plan.
